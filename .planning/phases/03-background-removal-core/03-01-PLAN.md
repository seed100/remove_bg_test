---
phase: 3
wave: 1
files_modified: ["package.json", "src/services/imageService.ts", "src/routes/imageRoutes.ts", "src/server.ts"]
autonomous: true
requirements_addressed: ["PROC-01", "PROC-02", "PROC-03"]
---

# Phase 3, Plan 1: Core Removal Logic

## Objective
Implement the background removal service and integrate it into the API route.

## Tasks

```xml
<task id="03-01-01" autonomous="true">
  <objective>Install background removal module and create service</objective>
  <action>
    Install `@imgly/background-removal-node`.
    Create `src/services/imageService.ts`.
    Implement `removeImageBackground(buffer: Buffer): Promise<Buffer>` using `removeBackground`.
  </action>
  <acceptance_criteria>
    - `package.json` contains `@imgly/background-removal-node`
    - `src/services/imageService.ts` correctly exports the removal function
  </acceptance_criteria>
</task>

<task id="03-01-02" autonomous="true">
  <objective>Integrate service into route and add model warm-up</objective>
  <read_first>
    - src/routes/imageRoutes.ts
    - src/server.ts
  </read_first>
  <action>
    Update `src/routes/imageRoutes.ts` to call `imageService.removeImageBackground`.
    Send the resulting buffer with `res.set('Content-Type', 'image/png').send(buffer)`.
    In `src/server.ts` (or `src/app.ts`), add a logic to trigger a dummy background removal call at startup to warm up the model.
  </action>
  <acceptance_criteria>
    - `POST /api/remove-bg` returns a binary image
    - Server logs "Model warmed up" or similar at startup
  </acceptance_criteria>
</task>

<task id="03-01-03" autonomous="true">
  <objective>Verify background removal with integration tests</objective>
  <read_first>
    - tests/upload.test.ts
  </read_first>
  <action>
    Create `tests/backgroundRemoval.test.ts`.
    Use a real (small) image file if possible, or a valid image buffer.
    Assert that the response content-type is `image/png` and the body is non-empty.
    Note: Initial test run might be slow due to model download.
  </action>
  <acceptance_criteria>
    - `tests/backgroundRemoval.test.ts` exists and passes
  </acceptance_criteria>
</task>
```
