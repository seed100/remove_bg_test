---
phase: 2
wave: 1
files_modified: ["package.json", "src/routes/imageRoutes.ts", "src/app.ts"]
autonomous: true
requirements_addressed: ["API-01", "API-02"]
---

# Phase 2, Plan 1: File Upload Implementation

## Objective
Implement the image upload endpoint with proper validation using Multer.

## Tasks

```xml
<task id="02-01-01" autonomous="true">
  <objective>Install multer and define upload middleware</objective>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install `multer` and `@types/multer`.
    Create `src/middlewares/upload.ts` to configure `multer` with `memoryStorage`.
    Add a `fileFilter` that only allows `image/jpeg` and `image/png`.
    Set a `limits` object with `fileSize: 10 * 1024 * 1024` (10MB).
  </action>
  <acceptance_criteria>
    - `package.json` contains `multer`
    - `src/middlewares/upload.ts` exists and uses `multer.memoryStorage()`
  </acceptance_criteria>
</task>

<task id="02-01-02" autonomous="true">
  <objective>Implement the upload route and register it in the app</objective>
  <read_first>
    - src/app.ts
  </read_first>
  <action>
    Create `src/routes/imageRoutes.ts`.
    Define `POST /remove-bg` route using the upload middleware: `upload.single('image')`.
    The route handler should verify if `req.file` exists (if not, throw 400).
    For now, return `{ message: "File received", filename: req.file.originalname }` as a placeholder.
    In `src/app.ts`, import `imageRoutes` and use it at `/api`.
  </action>
  <acceptance_criteria>
    - `src/routes/imageRoutes.ts` defines the POST route
    - `src/app.ts` contains `app.use('/api', imageRoutes)`
  </acceptance_criteria>
</task>

<task id="02-01-03" autonomous="true">
  <objective>Write integration tests for the upload endpoint</objective>
  <read_first>
    - src/app.ts
  </read_first>
  <action>
    Create `tests/upload.test.ts`.
    Test success case: Upload a small valid JPG/PNG (create a dummy buffer or use a small asset).
    Test error case: Upload no file (returns 400).
    Test error case: Upload invalid file type (e.g., .txt) (returns 400).
  </action>
  <acceptance_criteria>
    - `tests/upload.test.ts` exists
    - `npm test` passes all tests
  </acceptance_criteria>
</task>
```
